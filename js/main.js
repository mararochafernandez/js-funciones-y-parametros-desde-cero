'use strict';

// Fase 3

const writeThis = (word, times) => {
  for (let i = 0; i < times; i++) {
    console.log(word);
  }
};

writeThis('patata', 10);
writeThis('aguacate', 7);
writeThis('Pizza', 50);
